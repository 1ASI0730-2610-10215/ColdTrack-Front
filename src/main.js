/**
 * @summary Minimal Vue entry point for the clean integration project.
 * @author Codex Assistant
 */
import { createApp } from 'vue';
import './style.css';
import App from './App.vue';

createApp(App).mount('#app');
